# Data Folder

Restricted study data are not committed to this repository.

To run the adapted original R Markdown file in an authorized environment, place the required files here:

```text
data/private/cleaned_study_data.csv
data/private/symptom_severity_scores.csv
```

The `data/private/` folder is ignored by Git.
